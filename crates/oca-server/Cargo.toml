[package]
name = "oca-server"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
# Future: tokio, to spawn and supervise `opencode serve` for discovery, auto-start and the lock.
# Not added; T01 adds no logic.
